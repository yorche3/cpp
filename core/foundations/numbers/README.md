# Numbers — C++

Implementación de la especificación [04_Numbers](https://yorche3.github.io/programming_languages/core/foundations/04_Numbers/) en **C++**, utilizando **Bazelisk** (Bazel) y **Google Test** para las pruebas unitarias.

Este módulo implementa algoritmos fundamentales (suma de los primeros N naturales, factorial, Fibonacci, MCD, MCM) utilizando tres enfoques: **recursivo directo**, **recursivo con acumulador (tail recursion)** e **iterativo**.

---

## 📂 Archivos y estructura / Files & Structure

| Archivo / Directorio | Propósito |
|----------------------|-----------|
| [`include/numbers.h`](include/numbers.h) | Cabecera: Declaración de la clase `Numbers` y sus 20 métodos (15 públicos + 5 privados). |
| `src/numbers.cpp` | Implementación de las 15 funciones públicas (3 enfoques × 5 algoritmos) y 5 helpers privados. |
| `tests/numbers_iterative_tests.cpp` | Pruebas unitarias para las versiones iterativas (`...Iter`). |
| `tests/numbers_recursive_tests.cpp` | Pruebas unitarias para las versiones recursivas (`...Rec` y `...Acc`). |
| `BUILD` | Configuración de construcción de Bazel (targets `numbers_lib`, `numbers_iterative_tests`, `numbers_recursive_tests`). |
| `MODULE.bazel` | Gestión de dependencias externa (Google Test vía Bzlmod). |
| `WORKSPACE` | Raíz del espacio de trabajo (legacy/compatibility). |
| `.bazelversion` | Versión fija de Bazel para el proyecto. |
| `.gitignore` | Archivos y carpetas ignoradas (artefactos de compilación). |

**Estructura de directorios esperada:**

```text
numbers/
├── BUILD                 # Reglas de Bazel
├── MODULE.bazel          # Dependencias de Bazel
├── WORKSPACE             # Espacio de trabajo de Bazel
├── include/
│   └── numbers.h         # Declaraciones (interfaz)
├── src/
│   └── numbers.cpp       # Implementaciones
├── tests/
│   ├── numbers_iterative_tests.cpp
│   └── numbers_recursive_tests.cpp
├── .bazelversion
├── .gitignore
└── README.md
```

---

## 🛠️ Enfoque y construcción / Approach & Build

**ES:** El proyecto se configuró manualmente utilizando **Bazel** como sistema de construcción moderno. En lugar de compilar archivos individuales, Bazel gestiona la descarga de dependencias (Google Test) y la vinculación de la librería de forma automática.

**EN:** The project was manually configured using **Bazel** as a modern build system. Instead of compiling individual files, Bazel manages dependency downloads (Google Test) and library linking automatically.

### Pasos de inicialización:

1.  **Definir el módulo**: Configuración de `MODULE.bazel` para importar Google Test.
2.  **Configurar el BUILD**: Creación de un target `cc_library` para la lógica y dos `cc_test` para las pruebas (recursivas e iterativas).
3.  **Ejecución con Bazelisk**: Uso de `bazelisk` para garantizar que todos los desarrolladores usen la misma versión de Bazel.

---

## 📄 Archivos de configuración clave / Key Configuration Files

### `BUILD` – Configuración de build

**ES:** Define tres targets:
- `numbers_lib` (`cc_library`): compila la biblioteca con los fuentes y la cabecera.
- `numbers_recursive_tests` (`cc_test`): pruebas para los métodos recursivos (`...Rec` y `...Acc`).
- `numbers_iterative_tests` (`cc_test`): pruebas para los métodos iterativos (`...Iter`).

**EN:** Defines three targets:
- `numbers_lib` (`cc_library`): builds the library with sources and header.
- `numbers_recursive_tests` (`cc_test`): tests for recursive methods (`...Rec` and `...Acc`).
- `numbers_iterative_tests` (`cc_test`): tests for iterative methods (`...Iter`).

### `MODULE.bazel` – Manifiesto de dependencias

**ES:** Utiliza el nuevo sistema **Bzlmod** para declarar la dependencia de `googletest` versión `1.15.2`.
**EN:** Uses the new **Bzlmod** system to declare the `googletest` version `1.15.2` dependency.

```python
module(
    name = "cpp_numbers",
    version = "1.0",
)

bazel_dep(name = "googletest", version = "1.15.2", repo_name = "com_google_googletest")
```

### `.gitignore` – Archivos ignorados

**ES:** Patrones para no versionar artefactos de compilación generados por Bazel u otros compiladores.
**EN:** Patterns to avoid versioning build artifacts generated by Bazel or other compilers.

```gitignore
# Artefactos de compilación
build/
bin/
obj/
*.o
```

---

## 🚀 Compilación y ejecución / Build & Run

### Compilar / Build

```bash
bazelisk build //...
```

### Ejecutar pruebas / Run tests

```bash
# Ejecutar todas las pruebas
bazelisk test //...

# Ejecutar solo las pruebas iterativas
bazelisk test //:numbers_iterative_tests

# Ejecutar solo las pruebas recursivas
bazelisk test //:numbers_recursive_tests
```

**Salida esperada / Expected output:**

```text
INFO: Analyzed 2 targets (0 packages loaded, 0 targets configured).
INFO: Found 2 test targets...
PASS: //:numbers_iterative_tests (X.Xs)
PASS: //:numbers_recursive_tests (X.Xs)
INFO: Elapsed time: X.XXXs, Critical Path: X.XXs
INFO: 2 processes: 2 internal.
```

---

## 🧠 Algoritmos / operaciones

| Función / Algoritmo | Enfoques | Descripción |
|---------------------|----------|-------------|
| `sumOfFirstN` | `Rec`, `Acc`, `Iter` | Suma de los primeros N números naturales. |
| `factorial` | `Rec`, `Acc`, `Iter` | Factorial de un número N. |
| `fibonacci` | `Rec`, `Acc`, `Iter` | Enésimo número de la serie de Fibonacci. |
| `largestCommonDivisor` | `Rec`, `Acc`, `Iter` | Máximo Común Divisor (MCD / GCD). |
| `leastCommonMultiple` | `Rec`, `Acc`, `Iter` | Mínimo Común Múltiplo (MCM / LCM). |

---

## 📝 Notas de implementación / Implementation Notes

### 🔁 Sobre recursión con acumulador y Tail Call Optimization (TCO) / On recursion with accumulator and Tail Call Optimization (TCO)

**ES:**

*Tail recursion* ocurre cuando la llamada recursiva es la última acción que ejecuta una función/método; después de la llamada no hay más instrucciones, la función devuelve el resultado de la llamada recursiva. La recursión con acumulador consigue esto pasando el estado previo como parámetro a cada llamada, sin dejar trabajo pendiente en la pila.

En **C++**, **no** se garantiza TCO. El estándar de C++ no exige que los compiladores optimicen las llamadas recursivas de cola. Algunos compiladores (GCC, Clang) pueden realizarla en niveles de optimización altos (`-O2`, `-O3`) bajo ciertas condiciones, pero no es un comportamiento garantizado.

La implementación con acumulador se conserva únicamente con fines educativos: sirve como puente conceptual entre la recursión directa (más cercana a la definición matemática) y la versión iterativa (más eficiente). Como en este contexto no hay un beneficio práctico de rendimiento, no se desarrollan pruebas unitarias específicas para los métodos con acumulador. La validación del comportamiento se cubre a través de las pruebas de los enfoques recursivo e iterativo, que juntos ejercitan los mismos resultados.

**EN:**

*Tail recursion* occurs when the recursive call is the last action that runs a function/method; after the call there are no more instructions, the function returns the result of the recursive call. Recursion with accumulator gets this by passing the previous state as a parameter to each call, without leaving any pending work on the stack.

**C++ does not guarantee TCO.** The C++ standard does not require compilers to optimize tail-recursive calls. Some compilers (GCC, Clang) may perform it at high optimization levels (`-O2`, `-O3`) under certain conditions, but this behavior is not guaranteed.

The accumulator implementation is preserved only for educational purposes: it serves as a conceptual bridge between the direct recursive (closer to mathematical definition) and the iterative version (more efficient). Since there is no practical performance benefit, no specific unit tests are developed for the recursive methods with accumulator.

---

### 🌐 Otras implementaciones / Other implementations

Este proyecto también está implementado en otros lenguajes. Explora el [repositorio principal](https://github.com/yorche3/programming_languages) para ver todas las versiones.

---
*🌐 [github.com/yorche3/programming_languages](https://github.com/yorche3/programming_languages) · [GitHub Pages](https://yorche3.github.io/programming_languages/)*